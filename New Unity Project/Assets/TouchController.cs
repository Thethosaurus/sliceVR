using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Touch : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {
		// transform.localPosition = Quaternion.Slerp(OVRInput.GetLocalControllerPosition(OVRInput.Controller.RTouch));
		// transform.localRotation = Quaternion.Slerp(OVRInput.GetLocalControllerPosition(OVRInput.Controller.RTouch));
	}
}
